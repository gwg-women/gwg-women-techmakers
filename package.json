{
  "name": "mappa",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "cra-append-sw": "^2.1.1",
    "google-maps-react": "^1.1.2",
    "install": "^0.10.4",
    "npm": "^5.7.1",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.1",
    "serve": "^6.5.0"
  },
  "scripts": {
    "start": "react-scripts start && cra-append-sw --mode dev ./public/sw.js",
    "build": "react-scripts build && react-scripts build && cra-append-sw ./public/sw.js",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "now-start": "serve --single ./build"
  }
}
